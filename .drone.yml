image: dockerfile/nginx
script:
   - ls
   - sudo ifconfig 
   - echo hello world
publish:
   docker:
   docker_host: tcp://10.0.81.153:2375
   registry_login: false
   image_name:  python-env.init/tutor
   tag: last
notify:
  email:
    recipients:
       - 416205017@qq.com
    on_success: true
    on_failure: true
